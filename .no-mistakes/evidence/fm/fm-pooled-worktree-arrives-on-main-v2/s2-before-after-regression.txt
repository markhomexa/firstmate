================= REGRESSION: project registers [no-mistakes branch=develop] =================
--- BEFORE the change (product at base commit 1bb72cc) ---
registry: - yapii [no-mistakes branch=develop] - drive fixture (added 2026-09-18)
spawn exit=0 : spawned yapii-t1 harness=codex kind=ship mode=no-mistakes yolo=off window=firstmate:fm-yapii-t1 worktree=/tmp/nm-drive-s2.3hrEUR/pool
slot HEAD          = 119e166 foreign: billing change on main
slot == origin/develop ? NO
slot == origin/main    ? YES
foreign main commit 'billing.txt' inside the slot ? YES-FOREIGN-COMMITS
task record base_branch/base_registered: 

--- AFTER the change (product at 254f8ce) ---
registry: - yapii [no-mistakes branch=develop] - drive fixture (added 2026-09-18)
spawn exit=0 : spawned yapii-t1 harness=codex kind=ship mode=no-mistakes yolo=off window=firstmate:fm-yapii-t1 worktree=/tmp/nm-drive-s2.qTU1cw/pool
slot HEAD          = 262bbda develop: project working branch commit
slot == origin/develop ? YES
slot == origin/main    ? NO
foreign main commit 'billing.txt' inside the slot ? no
task record base_branch/base_registered: base_branch=develop base_registered=1 

================= UNCHANGED PATH: project registers no branch token =================
--- BEFORE the change ---
registry: - yapii [no-mistakes] - drive fixture (added 2026-09-18)
spawn exit=0 : spawned yapii-t1 harness=codex kind=ship mode=no-mistakes yolo=off window=firstmate:fm-yapii-t1 worktree=/tmp/nm-drive-s2.2NB0nO/pool
slot HEAD          = 0c502e1 foreign: billing change on main
slot == origin/develop ? NO
slot == origin/main    ? YES
foreign main commit 'billing.txt' inside the slot ? YES-FOREIGN-COMMITS
task record base_branch/base_registered: 

--- AFTER the change ---
registry: - yapii [no-mistakes] - drive fixture (added 2026-09-18)
spawn exit=0 : spawned yapii-t1 harness=codex kind=ship mode=no-mistakes yolo=off window=firstmate:fm-yapii-t1 worktree=/tmp/nm-drive-s2.pS2QvP/pool
slot HEAD          = a235ebf foreign: billing change on main
slot == origin/develop ? NO
slot == origin/main    ? YES
foreign main commit 'billing.txt' inside the slot ? YES-FOREIGN-COMMITS
task record base_branch/base_registered: base_branch=main 

=== launch brief the worker reads: BEFORE vs AFTER, project with NO registered branch ===
--- /tmp/nm-s2-old-plain.brief.md	2026-09-18 23:51:26.811148705 -0700
+++ /tmp/nm-s2-new-plain.brief.md	2026-09-18 23:51:30.276486750 -0700
@@ -2,7 +2,7 @@
 You are a crewmate: an autonomous worker agent managed by firstmate.
 This section establishes your current identity before every project or task instruction below and supersedes any conflicting role identity in those instructions.
 Do the assigned work yourself and report only to firstmate; do not adopt a firstmate or secondmate supervisor identity, delegate the task, run fleet supervision, or address the captain.
-Your steering inbox is `/tmp/nm-drive-s2.2NB0nO/home/state/yapii-t1.inbox`; this exact path belongs to your current task even when it is outside the worktree or under the supervising firstmate home, so read and acknowledge its messages and do not reject it as another home's state.
+Your steering inbox is `/tmp/nm-drive-s2.pS2QvP/home/state/yapii-t1.inbox`; this exact path belongs to your current task even when it is outside the worktree or under the supervising firstmate home, so read and acknowledge its messages and do not reject it as another home's state.
 Never inspect or change any other home's endpoint namespace; this authorization is limited to the exact task paths named by this brief.
 When this task works on Firstmate itself, the repository root `AGENTS.md` (also imported by `CLAUDE.md`) is project content and the supervisor contract for the firstmate managing you: follow this brief instead of that supervisor contract.
 Project instructions still govern the work wherever they do not conflict with this worker identity, including `CONTRIBUTING.md` and `firstmate-coding-guidelines` for Firstmate changes.
@@ -36,7 +36,7 @@
 2. Stay inside this worktree; modify nothing outside it.
 3. Use gh-axi for GitHub operations and chrome-devtools-axi for browser operations.
 4. Report status by appending one line:
-   `echo "{state}: {one short line}" >> '/tmp/nm-drive-s2.2NB0nO/home/state/yapii-t1.status'`
+   `echo "{state}: {one short line}" >> '/tmp/nm-drive-s2.pS2QvP/home/state/yapii-t1.status'`
    States: working, needs-decision, blocked, paused, done, failed.
    Each append wakes firstmate, so report sparingly: only phase changes a supervisor
    would act on (setup done, bug reproduced, fix implemented, validation passed) and the
@@ -54,8 +54,8 @@
 5. If you hit the same obstacle twice, append `blocked: {why}` and stop; firstmate will help.
 6. If a decision belongs above the implementation worker (product choices, destructive actions),
    append `needs-decision: {summary of options}` and stop. Firstmate will reply with the decision.
-   For a no-mistakes ask-user gate specifically, escalate all ask-user findings as one event plus one snapshot file, using that same shape even when the gate holds only a single ask-user finding: write only the ask-user findings, verbatim and unparaphrased (id, severity, file, line, description, authority), to `/tmp/nm-drive-s2.2NB0nO/home/data/yapii-t1/nm-<run>-findings.txt`, then report the gate with
-   `needs-decision [key=nm-<run>-<step>]: ask-user findings=<id1>,<id2>,... file=/tmp/nm-drive-s2.2NB0nO/home/data/yapii-t1/nm-<run>-findings.txt`
+   For a no-mistakes ask-user gate specifically, escalate all ask-user findings as one event plus one snapshot file, using that same shape even when the gate holds only a single ask-user finding: write only the ask-user findings, verbatim and unparaphrased (id, severity, file, line, description, authority), to `/tmp/nm-drive-s2.pS2QvP/home/data/yapii-t1/nm-<run>-findings.txt`, then report the gate with
+   `needs-decision [key=nm-<run>-<step>]: ask-user findings=<id1>,<id2>,... file=/tmp/nm-drive-s2.pS2QvP/home/data/yapii-t1/nm-<run>-findings.txt`
    naming every ask-user finding id from that gate. The status line only points at the file; it never restates or summarizes a finding's content.
    A decision or blocker you opened stays open until a `resolved` line carrying its exact key lands; a later `done:` or `working:` line never closes it, even when the answer is what started that work.
    Firstmate's reply normally writes that closing line at answer time; when a blocker or wait clears WITHOUT a firstmate reply, append `resolved: {how it cleared}` yourself (same `[key=<slug>]` if you opened it with one) as you resume.
@@ -73,15 +73,15 @@
    timed-out call was only waiting for a read while the run kept working.
 
 # Firstmate instruction inbox
-Firstmate steers you through durable message files in '/tmp/nm-drive-s2.2NB0nO/home/state/yapii-t1.inbox'.
-When a terminal message says an instruction is waiting there - and at any natural checkpoint when you are unsure - list '/tmp/nm-drive-s2.2NB0nO/home/state/yapii-t1.inbox'/*.msg, read and act on each message in numeric order, then acknowledge each handled message by moving it: `mv '/tmp/nm-drive-s2.2NB0nO/home/state/yapii-t1.inbox'/NNN.msg '/tmp/nm-drive-s2.2NB0nO/home/state/yapii-t1.inbox'/handled/`.
+Firstmate steers you through durable message files in '/tmp/nm-drive-s2.pS2QvP/home/state/yapii-t1.inbox'.
+When a terminal message says an instruction is waiting there - and at any natural checkpoint when you are unsure - list '/tmp/nm-drive-s2.pS2QvP/home/state/yapii-t1.inbox'/*.msg, read and act on each message in numeric order, then acknowledge each handled message by moving it: `mv '/tmp/nm-drive-s2.pS2QvP/home/state/yapii-t1.inbox'/NNN.msg '/tmp/nm-drive-s2.pS2QvP/home/state/yapii-t1.inbox'/handled/`.
 The move IS the acknowledgement: without it firstmate rings again and eventually treats you as stuck. An empty or absent inbox needs no action.
 
 # Project memory
-If `AGENTS.md` or `CLAUDE.md` already exists, or if this task produced durable project-intrinsic knowledge, run `/tmp/nm-base-product/bin/fm-ensure-agents-md.sh .` in the worktree.
+If `AGENTS.md` or `CLAUDE.md` already exists, or if this task produced durable project-intrinsic knowledge, run `/home/homexa/.no-mistakes/worktrees/a20fe525da72/01M2W1CD32TK88D266F4E2FCPB/bin/fm-ensure-agents-md.sh .` in the worktree.
 Record only project knowledge useful to almost every future session.
 For anything the codebase already shows, prefer a pointer to the authoritative file, command, or doc over copying the detail.
-If you touch a project `AGENTS.md`, follow `/tmp/nm-base-product/bin/fm-ensure-agents-md.sh`'s self-governance contract in the same pass.
+If you touch a project `AGENTS.md`, follow `/home/homexa/.no-mistakes/worktrees/a20fe525da72/01M2W1CD32TK88D266F4E2FCPB/bin/fm-ensure-agents-md.sh`'s self-governance contract in the same pass.
 Keep it proportionate: skip `AGENTS.md` edits for trivial tasks that produced no durable project knowledge.
 
 # Definition of done

=== launch brief diff, project WITH a registered working branch (BEFORE vs AFTER) ===
--- /tmp/nm-s2-old-registered.brief.md	2026-09-18 23:51:19.902336575 -0700
+++ /tmp/nm-s2-new-registered.brief.md	2026-09-18 23:51:22.963251013 -0700
@@ -2,7 +2,7 @@
 You are a crewmate: an autonomous worker agent managed by firstmate.
 This section establishes your current identity before every project or task instruction below and supersedes any conflicting role identity in those instructions.
 Do the assigned work yourself and report only to firstmate; do not adopt a firstmate or secondmate supervisor identity, delegate the task, run fleet supervision, or address the captain.
-Your steering inbox is `/tmp/nm-drive-s2.3hrEUR/home/state/yapii-t1.inbox`; this exact path belongs to your current task even when it is outside the worktree or under the supervising firstmate home, so read and acknowledge its messages and do not reject it as another home's state.
+Your steering inbox is `/tmp/nm-drive-s2.qTU1cw/home/state/yapii-t1.inbox`; this exact path belongs to your current task even when it is outside the worktree or under the supervising firstmate home, so read and acknowledge its messages and do not reject it as another home's state.
 Never inspect or change any other home's endpoint namespace; this authorization is limited to the exact task paths named by this brief.
 When this task works on Firstmate itself, the repository root `AGENTS.md` (also imported by `CLAUDE.md`) is project content and the supervisor contract for the firstmate managing you: follow this brief instead of that supervisor contract.
 Project instructions still govern the work wherever they do not conflict with this worker identity, including `CONTRIBUTING.md` and `firstmate-coding-guidelines` for Firstmate changes.
@@ -36,7 +36,7 @@
 2. Stay inside this worktree; modify nothing outside it.
 3. Use gh-axi for GitHub operations and chrome-devtools-axi for browser operations.
 4. Report status by appending one line:
-   `echo "{state}: {one short line}" >> '/tmp/nm-drive-s2.3hrEUR/home/state/yapii-t1.status'`
+   `echo "{state}: {one short line}" >> '/tmp/nm-drive-s2.qTU1cw/home/state/yapii-t1.status'`
    States: working, needs-decision, blocked, paused, done, failed.
    Each append wakes firstmate, so report sparingly: only phase changes a supervisor
    would act on (setup done, bug reproduced, fix implemented, validation passed) and the
@@ -54,8 +54,8 @@
 5. If you hit the same obstacle twice, append `blocked: {why}` and stop; firstmate will help.
 6. If a decision belongs above the implementation worker (product choices, destructive actions),
    append `needs-decision: {summary of options}` and stop. Firstmate will reply with the decision.
-   For a no-mistakes ask-user gate specifically, escalate all ask-user findings as one event plus one snapshot file, using that same shape even when the gate holds only a single ask-user finding: write only the ask-user findings, verbatim and unparaphrased (id, severity, file, line, description, authority), to `/tmp/nm-drive-s2.3hrEUR/home/data/yapii-t1/nm-<run>-findings.txt`, then report the gate with
-   `needs-decision [key=nm-<run>-<step>]: ask-user findings=<id1>,<id2>,... file=/tmp/nm-drive-s2.3hrEUR/home/data/yapii-t1/nm-<run>-findings.txt`
+   For a no-mistakes ask-user gate specifically, escalate all ask-user findings as one event plus one snapshot file, using that same shape even when the gate holds only a single ask-user finding: write only the ask-user findings, verbatim and unparaphrased (id, severity, file, line, description, authority), to `/tmp/nm-drive-s2.qTU1cw/home/data/yapii-t1/nm-<run>-findings.txt`, then report the gate with
+   `needs-decision [key=nm-<run>-<step>]: ask-user findings=<id1>,<id2>,... file=/tmp/nm-drive-s2.qTU1cw/home/data/yapii-t1/nm-<run>-findings.txt`
    naming every ask-user finding id from that gate. The status line only points at the file; it never restates or summarizes a finding's content.
    A decision or blocker you opened stays open until a `resolved` line carrying its exact key lands; a later `done:` or `working:` line never closes it, even when the answer is what started that work.
    Firstmate's reply normally writes that closing line at answer time; when a blocker or wait clears WITHOUT a firstmate reply, append `resolved: {how it cleared}` yourself (same `[key=<slug>]` if you opened it with one) as you resume.
@@ -73,15 +73,15 @@
    timed-out call was only waiting for a read while the run kept working.
 
 # Firstmate instruction inbox
-Firstmate steers you through durable message files in '/tmp/nm-drive-s2.3hrEUR/home/state/yapii-t1.inbox'.
-When a terminal message says an instruction is waiting there - and at any natural checkpoint when you are unsure - list '/tmp/nm-drive-s2.3hrEUR/home/state/yapii-t1.inbox'/*.msg, read and act on each message in numeric order, then acknowledge each handled message by moving it: `mv '/tmp/nm-drive-s2.3hrEUR/home/state/yapii-t1.inbox'/NNN.msg '/tmp/nm-drive-s2.3hrEUR/home/state/yapii-t1.inbox'/handled/`.
+Firstmate steers you through durable message files in '/tmp/nm-drive-s2.qTU1cw/home/state/yapii-t1.inbox'.
+When a terminal message says an instruction is waiting there - and at any natural checkpoint when you are unsure - list '/tmp/nm-drive-s2.qTU1cw/home/state/yapii-t1.inbox'/*.msg, read and act on each message in numeric order, then acknowledge each handled message by moving it: `mv '/tmp/nm-drive-s2.qTU1cw/home/state/yapii-t1.inbox'/NNN.msg '/tmp/nm-drive-s2.qTU1cw/home/state/yapii-t1.inbox'/handled/`.
 The move IS the acknowledgement: without it firstmate rings again and eventually treats you as stuck. An empty or absent inbox needs no action.
 
 # Project memory
-If `AGENTS.md` or `CLAUDE.md` already exists, or if this task produced durable project-intrinsic knowledge, run `/tmp/nm-base-product/bin/fm-ensure-agents-md.sh .` in the worktree.
+If `AGENTS.md` or `CLAUDE.md` already exists, or if this task produced durable project-intrinsic knowledge, run `/home/homexa/.no-mistakes/worktrees/a20fe525da72/01M2W1CD32TK88D266F4E2FCPB/bin/fm-ensure-agents-md.sh .` in the worktree.
 Record only project knowledge useful to almost every future session.
 For anything the codebase already shows, prefer a pointer to the authoritative file, command, or doc over copying the detail.
-If you touch a project `AGENTS.md`, follow `/tmp/nm-base-product/bin/fm-ensure-agents-md.sh`'s self-governance contract in the same pass.
+If you touch a project `AGENTS.md`, follow `/home/homexa/.no-mistakes/worktrees/a20fe525da72/01M2W1CD32TK88D266F4E2FCPB/bin/fm-ensure-agents-md.sh`'s self-governance contract in the same pass.
 Keep it proportionate: skip `AGENTS.md` edits for trivial tasks that produced no durable project knowledge.
 
 # Definition of done
@@ -117,6 +117,13 @@
 
 After /no-mistakes reports CI green (the CI-ready return point - do not wait for it to keep monitoring in the background until merge), append `done: PR {url} checks green` and stop. You are finished.
 
+# Current worktree base contract
+This worktree is based on `develop`, the working branch registered for this project.
+This section establishes that base after every Setup or promotion instruction above and supersedes any of them that names a different base, including any that calls this worktree's base the default branch.
+The no-mistakes pipeline opens this task's PR, and nothing in this brief sets the base it opens against.
+Read that PR's own base branch as soon as the PR exists, before you report `done: PR {url} checks green`.
+If it is anything other than `develop`, append `blocked: PR base is {branch}, not develop` to the status file and stop, because a PR raised against another branch would show that branch's own commits as this task's change.
+
 # Current no-mistakes intent contract
 This section supersedes every earlier brief instruction about constructing `--intent`, but not later clarifications actually supplied by the captain.
 Use everything under `## Captain intent authorized for --intent` through the end of this brief, including any nested subheadings but excluding that heading, plus any later words the captain actually supplied as `--intent`; never include Firstmate specification or other mixed Task content.
